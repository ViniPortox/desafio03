import 'package:flutter/material.dart';

class Testzin extends StatelessWidget {
  const Testzin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('oiiii');
  }
}
